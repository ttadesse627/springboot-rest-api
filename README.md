# springboot-rest-api
Java Spring boot REST API  Exploration project

When setting the objects properties we may validate the field before setting the value. That is the advantage of the 
setter and getter way of assigning properties
